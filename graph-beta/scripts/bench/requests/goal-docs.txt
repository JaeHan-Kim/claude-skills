Document this repository — a Node monorepo `tinyq` with three packages under packages/ (queue, retry, worker), tests, and no docs — so that a new maintainer can use it, extend it and understand why it is built the way it is. You decide the document set, its structure and where each document lives; derive every claim from the code, and review each document against the source before you call it done. Do not change any source or test file.
